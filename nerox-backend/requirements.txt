fastapi>=0.115.0
uvicorn[standard]>=0.29.0
gunicorn>=23.0.0
pymongo>=4.7.2
motor>=3.7.0
python-dotenv>=1.0.1
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
pydantic[email]>=2.10.0
email-validator>=2.1.1
pydantic-settings>=2.3.0
python-multipart>=0.0.9
redis>=5.0.0
rq>=1.16.2
boto3>=1.35.0
stripe>=11.0.0
playwright>=1.52.0

# ── Phase 3: AI Fingerprinting & Detection Engine ──────────────────────────
# CPU-only PyTorch — Railway has no GPU; CUDA wheels add ~4 GB unnecessarily.
# Pinned to exact versions for reproducible Railway builds.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2

opencv-python-headless>=4.8.0
faiss-cpu>=1.7.4
# Pin numpy<2 — torch==2.2.2 was compiled against NumPy 1.x ABI.
# NumPy 2.x breaks torch, faiss, and opencv at import time.
numpy>=1.24.0,<2.0.0

# ── Phase 5: Invisible Watermarking + Ownership Trace ──────────────────────
# Uses existing deps: opencv-python (DCT transforms), numpy, hashlib/hmac (stdlib)
# No additional packages required.

# ── Phase 2.5: Auto-Detection Engine ──────────────────────────────────────
# Uses existing deps: numpy, opencv-python, faiss-cpu, torch, torchvision
# Ingestion layer uses stdlib: urllib, html.parser, json
# No additional packages required.

